# Whitelist remote image sources for Netlify's Image CDN (used by @nuxt/image
# on Netlify). Only APOD images are rendered — videos are skipped at ingest.
[images]
  remote_images = [
    'https://apod.nasa.gov/.*'
  ]
